import { Header } from "@/layouts/header";

const HomePage = () => {
  return (
    <div className="h-screen w-screen">
      <Header></Header>
      <span>Content</span>
    </div>
  );
};

export default HomePage;
